import Layout from "@/components/template/Layout";
// import { AppConsumer } from "@/data/context/AppContext";

export default function Notificacoes() {
    return (
        <Layout
            titulo="Notificações"
            subtitulo="Mensagens recebidas"
        >
            {/*
            <AppConsumer>
                {dados => <h3>{dados.tema}</h3>}
            </AppConsumer>
            */}
        </Layout>
    );
}
